{{/*
Intentionally empty.

When redis.enabled=true, the Bitnami Redis sub-chart auto-generates and manages
the password under Secret "{{ .Release.Name }}-redis" (key "redis-password").
The API reads it via valueFrom in deployment.yaml.

When redis.enabled=false, the user provides REDIS_PASSWORD as part of
secrets.data (or secrets.existingSecret), and it is injected via envFrom.
*/}}
