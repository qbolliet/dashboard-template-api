---
id: graphql-api-index
title: GraphQL API Reference
sidebar_label: Overview
slug: /graphql-api
---

# GraphQL API Reference

This section is generated automatically from the GraphQL schema by
[`@graphql-markdown/docusaurus`](https://graphql-markdown.dev/).

If you are seeing this placeholder it means the generator has not run yet.

To populate the section, from the repository root:

```bash
npm run build && npm run docs:schema
cd docs-site && npm run build
```

Or run the docusaurus generator manually:

```bash
cd docs-site && npx docusaurus graphql-to-doc
```
