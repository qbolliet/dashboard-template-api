// Post-framework setup (setupFilesAfterEnv) — runs after Jest globals are injected.
// Environment variables and console overrides are already configured in setup-env.js (setupFiles).
// Add Jest-specific hooks here (e.g. global beforeEach/afterEach) if needed in the future.
