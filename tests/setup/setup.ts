/**
 * Post-framework Jest setup file.
 *
 * Executed after Jest globals are injected (setupFilesAfterEnv).
 * Environment variables and console overrides are configured in setup-env.ts (setupFiles).
 * Add Jest-specific hooks here (e.g. global beforeEach/afterEach) if needed.
 */

// Configuration post-framework — exécution après l'injection des globals Jest.
// Variables d'environnement et substitutions de console définies dans setup-env.ts.
// Ajout de hooks globaux Jest ici si nécessaire à l'avenir.
